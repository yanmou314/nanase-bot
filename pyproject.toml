[project]
name = "qq-bot"
version = "0.1.0"
description = "QQ Bot based on NoneBot2"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.nonebot]
plugin_dirs = ["plugins"]
