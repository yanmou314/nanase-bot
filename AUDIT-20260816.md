# QQ 机器人安全与代码审计报告(2026-08-16)

范围:`/opt/bot`(NoneBot2 + OneBot V11,14 个插件)、NapCat QQ 客户端、systemd 服务编排。
基线:改动前全量备份于 `/root/maintenance-20260816/`(bot 代码 tar、NapCat 配置 tar、git 基线)。

## 一、本次已修复(commit b8a11c6)

| # | 问题 | 等级 | 修复 |
|---|------|------|------|
| 1 | auto_chat/random_chat 的 AI 与 qingyunke 兜底回复以纯字符串发送,OneBot v11 会把其中 `[CQ:...]` 解析为真实 CQ 码(第三方文本可注入图片/@等) | 高 | 全部改用 `MessageSegment.text()` 包裹 |
| 2 | random_chat 跨插件引用 auto_chat 私有符号 `_load_key()`、`_AI_SEM`、`API_URL`、`MODEL`,脆弱耦合 | 中 | auto_chat 新增公开接口 `chat_completion()` / `get_api_key()`,random_chat 仅用公开 API |
| 3 | llm_price 注释承诺"代理不可用回退直连"但未实现,Clash 停机时无法更新价格 | 中 | `_fetch_api_json()` 捕获 ProxyError/ConnectError/ConnectTimeout 后直连重试;`MODELS_DEV_PROXY=""` 可强制直连 |
| 4 | USD_CNY=7.2 硬编码 | 低 | 支持 `USD_CNY` 环境变量覆盖,默认 7.2 不变 |
| 5 | qingyunke 兜底响应未校验结构,异常数据可能成为回复 | 低 | 严格校验 `result==0` 且 `content` 为字符串,回复截断 300 字符(实测该 API 无 HTTPS,明文为唯一可用通道,已注释说明) |
| 6 | request_manager 同意/拒绝 API 调用失败时,该申请从待处理列表丢失,无法重试 | 中 | 失败时放回 `_pending` |
| 7 | auto_chat 文件头 UTF-8 BOM | 低 | 已清除 |

## 二、本次已加固(服务器配置,保守模式)

| # | 项目 | 动作 |
|---|------|------|
| 1 | NapCat WebUI(6099)弱 token(12 位 hex) | 更换为 32 位随机 hex |
| 2 | OneBot WS(NapCat→NoneBot)空 token | 双端(NapCat onebot11 配置 + NoneBot `.env` 的 `ONEBOT_ACCESS_TOKEN`)设置同一 32 位随机 token |
| 3 | 密钥文件全局可读(644) | `chmod 600`:`.env`、`plugins/auto_chat/config.json`、`plugins/chat_stats/db.json`、NapCat `webui.json`、`onebot11_1837204555.json` |

新 token 备份于服务器 `/root/maintenance-20260816/new-tokens.txt`(600 权限)。

## 三、遗留建议(本次未执行,按需采纳)

1. **服务降权**:qqbot/napcat/overstats/clash 均以 root 运行。建议创建专用用户运行 bot 与 overstats(NapCat 因 Electron/xvfb 依赖较复杂,可后置)。风险:文件属主迁移需停机操作。
2. **NapCat WebUI 与 Clash 改绑 127.0.0.1**:两者目前监听所有网卡,依赖阿里云安全组拦截。改绑后远程访问需走 SSH 隧道(`ssh -L 6099:127.0.0.1:6099`)。
3. **启用 ufw**(默认拒绝入站,放行 22):作为安全组之外的第二道防线。
4. **轮换 opencode.ai API key**(`plugins/auto_chat/config.json` 中的 sk- 开头密钥):该密钥曾以 644 权限长期存在于磁盘,建议在 provider 侧作废重发。
5. **轮换 PostgreSQL 密码**(chat_stats/db.json 中的 DSN):同上。PG 仅监听 127.0.0.1,实际风险低。
6. **qingyunke 兜底仍为明文 HTTP**:该免费 API 无 TLS,存在理论上的中间人篡改风险;目前已做输入截断、结构校验与 CQ 注入防护。若在意可删除该兜底,直接使用本地语料 FALLBACKS。
7. **调休数据缺失**:holiday_countdown 的 `_is_workday` 不识别调休补班的周末(数据表只有节假日,无调休标记),调休周六会误显示"上班倒计时"。需补录每年调休安排后修复。
8. **chat_stats 隐私**:群聊消息明文存 PostgreSQL(截断 200 字符,保留 30 天),属设计选择;如需增强可考虑哈希化或缩短保留期。

## 四、安全态势快照(审计时点)

- 监听端口:22(公开)、6099 WebUI(全网卡,弱 token 已换强)、7890/7891/7892 clash(全网卡)、8080/5432/18080 仅回环 ✅
- ufw 未启用,防线依赖阿里云安全组(服务器内无法核验其规则)
- 源库只读、参数化 SQL、HTML 转义、SSRF fetcher 白名单(仅 data:)、原子 JSON 写、OOM 防护与 napcat-guard 自动重启 —— 代码层工程化程度良好
- git 工作区已 clean;`.gitignore` 覆盖全部运行时密钥文件 ✅
