# 代码优化审查报告（2026-08-23）

> 审查对象：/opt/bot（NoneBot2 QQ 机器人，14 个启用插件，约 7000 行）
> 审查方式：4 个并行 agent 分别审查核心架构 / 数据统计插件 / 网络定时插件 / 测试工程
> 环境：Ubuntu 24.04 · 2 核 / 1.6GB 内存 · Python 3.12 · PostgreSQL · NapCatQQ

## 总体结论

代码质量**明显高于同类个人项目**：HTTP 全异步带超时、状态文件原子写、渲染走线程池、SSRF/XSS 有防护、时区全链路统一、测试 118 项且断言质量高。主要问题集中在：**3 个高危缺陷**（依赖缺失、密钥泄露风险、CQ 注入回归）、**数据库故障路径**、**事件循环内同步 IO**、**访问控制信息回显到群聊**、**测试覆盖不均**。

---

## 🔴 高危问题（建议立即修复）

### H1. requirements.txt 缺 cnlunar 依赖
`plugins/news/__init__.py:10` 硬依赖 `cnlunar` 但未写入 requirements.txt。
实测干净环境 `pytest` 直接 collection error 中断；按 README 重新部署生产环境时 news 插件无法加载。
**修复**：requirements.txt 补 `cnlunar`；建议加干净 venv 安装+跑测试的 CI。

### H2. .gitignore 漏掉 ai_config.json（含 API key）
`.gitignore:13` 的 `plugins/**/config.json` 不匹配 `plugins/news/ai_config.json`（实测存在且含 api_key 字段），README 声称"已排除全部敏感文件"与事实不符。
**修复**：.gitignore 加 `plugins/**/ai_config.json`；检查 git 历史是否已提交过该文件，如有需轮换 key。

### H3. news 晨报 CQ 注入回归
`plugins/news/__init__.py:390`（_send_daily）与 `:430`（news_test）以纯 `str` 发送一言 API / 60s 新闻 / AI 生成内容。OneBot v11 会把文本中的 `[CQ:...]` 解析为真实 CQ 码（图片/@/转发）。这是上次审计 #1 修复过的问题在 8/23 新功能中回归。
**修复**：两处改为 `MessageSegment.text(text)`。

### H4. 连接池初始化失败后残留坏池，永不自愈
`plugins/chat_stats/db_pg.py:45-46` — `_pool` 先赋值全局变量，之后 `open()` 或建表 DDL 失败（如 PG 未就绪时 bot 重启）则后续所有 `get_pool()` 返回坏池：每次查询等 30 秒超时，消息队列堆满持续丢弃，直到重启进程。
**修复**：open + DDL 包 try/except，失败时 `await _pool.close()` 并置 `_pool = None` 后再 raise，允许下次重试。

### H5. DB 查询异常被吞成"无数据"
`plugins/chat_stats/db_pg.py:110-114` — `exec()` 捕获 `fetchall()` 异常返回 `[]`，PoolTimeout 等故障被伪装成"今天还没有人发言哦～"，掩盖真实故障；而 execute 异常又直接冒泡导致用户无任何回复，两条路径行为不一致。
**修复**：删除 try/except 统一上抛，在命令 handler 捕获后回复"统计服务暂时不可用"。

### H6. 核心审批流零测试
`plugins/request_manager/__init__.py:283-369` — `_process_decision`/`_auto_approve` 完全无测试，其中 :368"失败放回 _pending"正是上次审计 #6 的修复，无回归守护；群内多申请仲裁、引用消息解析正则同样裸奔。

---

## 🟡 中危问题

### 访问控制与信息安全
- **M1** `request_manager:146,160,185,211` — 自动通过关键字回显到群聊，全群看到"进群暗号"后任何人可伪造附言拉号进群，审核被绕过 → 改私发。
- **M2** `help/__init__.py:200-212` — owner 变体帮助图发送到事件上下文，群里 `.hp` 会把全部管理命令公开广播，与"仅你可见"矛盾 → 改私发。
- **M3** `common.py:80-88` — `load_json_state` 对损坏 JSON 静默返回 `{}`，之后一次 save 即清空原配置（影响 6 个插件）；`save_json_state` 无 fsync → 移植 request_manager 的 corrupt-backup 逻辑 + 写前 fsync。

### 事件循环阻塞（1.6GB/2 核机器敏感）
- **M4** `common.py:33-40,52-71` — `save_image`/`cleanup_cache` 同步 IO + 全目录扫描，被 owstats:366,398,444 在 async handler 直接调用；chat_stats 词云路径同样 → 提供 `asyncio.to_thread` 封装。
- **M5** `common.py:104-127` — `render_html_to_png` 重型同步管线无 async 封装无警示；毫秒时间戳文件名并发碰撞会互相覆盖输出 → 加 uuid 后缀 + async 封装。
- **M6** `group_leave:107-140` — 每次入群全量同步写盘（上限 10 万条 indent=2 的 JSON 可达数 MB）；bot 连接后逐群导入每个群都全量写一次盘 → 快照 + to_thread + 导完只写一次。
- **M7** 渲染并发无上限（chat_stats:130、dragon_card.py:121、cmd_stats:306）— 多群同时触发词云/龙王时 weasyprint + pdftoppm 并发可打爆 1.6GB 内存 → 全局 `asyncio.Semaphore(1)` 串行化渲染；`-r 192` 可降到 144。

### 数据库与统计
- **M8** `chat_stats/__init__.py:106-113` — 词云查询一次 `fetchall()` 全天文本在 Python 侧分词，活跃大群数万条消息全部载入内存 → 服务端游标流式处理；SQL 的 OR 条件在 WORDS_CUTOFF_HOUR=0 时恒假可简化。
- **M9** `chat_stats:212 + cmd_stats:311` — 两个日报任务同在 00:00 触发形成 CPU/内存尖峰；且 00:00:00 立即查"昨天"会系统性缺失 23:59:59.5 后未落库的消息（0.5s flush 窗口）→ 错峰（00:02/00:05）+ 任务开头先 `await _write_queue.join()`。
- **M10** `owstats:141-148` — 预热与用户查询竞态：预热只在每玩家开始前检查 `OW_LOCK.locked()`，之后到达的用户查询会与预热并发挤占上游 5 req/s 配额。

### 网络与定时任务
- **M11** `auto_chat:284-287,239-257` — `except httpx.ReadTimeout` 捕不到 `ConnectTimeout`（代理失效时最常见），落入通用分支 owner 收不到通知；且通知文案"已切换到青云客备用接口"与实际行为（直接不回复）不符，qingyunke 已于 8/23 移除 → 改捕获 `httpx.TimeoutException` + 改文案。
- **M12** `auto_chat:124-160` — 重试不区分错误类型，401/400 等不可重试错误也重试 3 次，最坏 ~92 秒占用并发槽；`_AI_SEM(3)` 被 3 个超时请求占满后所有 AI 聊天挂起近 1.5 分钟 → 4xx 不重试 + 指数退避 + 单次超时降至 15s。
- **M13** `news:261-265 / repeater:58-72` — 两处自实现非原子写盘（tmp+os.replace 的现成工具没用），OOM/断电留下截断 JSON → 改用 `common.save_json_state`。
- **M14** 每日推送任务无"当日已推送"标记（bot.py:18-24 + news:395 + holiday:396）— APScheduler 内存 jobstore，进程重启错过的 7:00/17:00 不补发；事件循环卡顿超 misfire_grace_time 当日静默跳过 → state 记录 last_push_date + 启动补发，或推送失败接入 error_notify。

### 测试与工程
- **M15** `tests/conftest.py:163-174` — v11 stub 缺 `GroupDecreaseNoticeEvent`/`GroupIncreaseNoticeEvent`/`on_bot_connect`，group_leave 实测无法加载测试；MessageSegment 无 `__add__`。
- **M16** README 与代码多处脱节：llm_price 位置（已移到 plugins-disabled）、目录漏 random_chat/what_to_eat、仍宣传已禁用的 `.ai` 命令、Python 版本要求矛盾（README ≥3.9 vs pyproject ≥3.10）、缺"如何运行测试"章节。
- **M17** 插件导入时读生产状态文件污染测试初始状态（repeater:75、random_chat:80）；test_random_chat 无 autouse 清理 fixture，`_buffers` 跨测试泄漏。

---

## 🟢 低危问题（择要）

| 位置 | 问题 | 建议 |
|------|------|------|
| db_pg.py:105 | SELECT 不 commit，每次读白开事务 | 建池传 `autocommit=True` |
| db_pg.py:136 | executemany 批写 | 量大后可换 COPY |
| chat_stats:53 | 无上限 DELETE 长事务 | 按主键分批删 |
| cmd_stats:203 | TOP5 昵称串行 await | asyncio.gather 并行 |
| common.py:130 | 渐变背景逐像素行循环 | 画 1×H 条再 resize |
| common.py:151 | HTTP 客户端隐式依赖环境变量代理 | systemd unit 固化 `Environment=HTTPS_PROXY=...` |
| common.py:148 | shutdown 关闭依赖各插件恰好注册 | common 内注册 `driver.on_shutdown(close_http_clients)` |
| request_manager:347 | 并发"同意"触发 KeyError | `pop(key, None)` + 提示已被处理 |
| request_manager:317 | 同群多申请引用仲裁可能选错 | 通知嵌短码精确匹配 |
| request_manager:270 等 | 多处 `except: pass` 无日志 | 换 warning + exc_info |
| help:156 | 字体缺失回退不含 CJK 的默认字体发乱码图 | 直接 raise 走文本回退 |
| error_notify:133 | run_coroutine_threadsafe 的 future 异常被吞 | add_done_callback 记日志 |
| random_chat:146 | 插话无同群最小间隔，可能短时连发 | 触发前查 60s 间隔 |
| random_chat:89 | 缓冲驱逐按插话时间，活跃群被误驱逐 | 维护 _last_seen |
| repeater:91 | prune 注释与实现语义不符 | 记录最后消息时间戳 |
| news:244 | key 加载失败静默清空无日志 | warning 去重日志 |
| owstats:242 | 排队无上限无位置反馈 | 队列上限 + "前方 N 人" |
| db_pg 相关 | 词云滑窗注释与实现不符（步长 4 分块丢尾字符） | 改步长 2 或修注释 |
| 多处 | 死代码：dragon_card.py:36 share、wordcloud_card.py:58 msg_count、chat_stats msg_type | 清理或利用 |

## 工程卫生

1. **22 个 .bak 文件**：已被 git 忽略但磁盘残留，生产已是 git 仓库 → 批量删除（git 历史即备份）。
2. **.deleted/（1.2MB png）**：纯垃圾 → 删除。
3. **plugins-disabled/llm_price/cache/**：`plugins/**/cache/` 不匹配该路径会被提交，含运行时产物 → 清空 + .gitignore 补 `plugins-disabled/**/cache/`。
4. **.gitignore 补漏**：`.pytest_cache/`、`*.corrupt-*`（含 QQ 号）、`plugins/**/ai_config.json`。
5. **llm_price_test.py** 在 plugins-disabled 下永不被收集（testpaths=tests）→ 移入 tests/ 或删除。
6. **无 lint/CI**：建议 ruff + GitHub Actions（干净 venv 安装 + pytest），可直接拦住 cnlunar 类问题。
7. **DEPLOY.md 仅 7 行**：缺 poppler-utils、字体、PostgreSQL、venv、systemd unit、clash 依赖说明，无法照此重建环境。

## 上次审计（AUDIT-20260816）解决情况

8 项主要问题中 6 项已确认解决（跨插件私有符号、USD_CNY、BOM、repeater None 指纹、调休数据、qingyunke 以移除方式解决），CQ 注入部分回归（H3），申请失败放回已修但无回归测试（H6）。遗留的服务器侧建议（token 轮换、ufw、非 root 运行）无代码层证据，需上服务器核验。

## 建议修复顺序

1. **立即**：H1（依赖）、H2（密钥）、H3（注入）— 半小时内可完成
2. **本周**：H4/H5（DB 故障路径）、M1/M2（权限回显）、M11/M12（AI 聊天故障场景）
3. **逐步**：M4-M7（事件循环）、M8-M10（资源峰值）、测试补齐（H6/M15）
4. **随手**：低危项与工程卫生
